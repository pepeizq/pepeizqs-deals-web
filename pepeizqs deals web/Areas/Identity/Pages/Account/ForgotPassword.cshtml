@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = Herramientas.Idiomas.CogerCadena(Model.idioma, "ForgotPassword.String1");
}

@(await Html.RenderComponentAsync<pepeizqs_deals_web.Areas.Identity.Componentes.ContrasenaOlvidada>(RenderMode.ServerPrerendered, new { idioma = @Model.idioma }))

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
