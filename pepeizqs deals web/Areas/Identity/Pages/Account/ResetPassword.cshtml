@page
@model ResetPasswordModel
@{
    ViewData["Title"] = Herramientas.Idiomas.CogerCadena(Model.idioma, "ResetPassword.String1");
}

@(await Html.RenderComponentAsync<pepeizqs_deals_web.Areas.Identity.Componentes.ContrasenaResetear>(RenderMode.ServerPrerendered, new { idioma = Model.idioma, codigo = Model.codigo }))

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
