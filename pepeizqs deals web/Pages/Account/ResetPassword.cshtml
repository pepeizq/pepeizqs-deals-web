@page
@model pepeizqs_deals_web.Pages.Account.ResetPasswordModel
@{
    ViewData["Title"] = Herramientas.Idiomas.BuscarTexto(Model.idioma, "String1", "ResetPassword");
    Layout = "/Pages/Shared/_Layout.cshtml";
}

@(await Html.RenderComponentAsync<Componentes.Cuenta.ContrasenaResetear>(RenderMode.ServerPrerendered, new { 
    idioma = Model.idioma, 
    codigo = Model.codigo 
}))

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}