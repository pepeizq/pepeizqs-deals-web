@page "/news/{id:int}/{nombre?}/"

@model pepeizqs_deals_web.Pages.NewsModel

@{
	#nullable disable

	ViewData["Title"] = Herramientas.Idiomas.MirarTexto(Model.idioma, Model.noticia.TituloEn, Model.noticia.TituloEs);
	Layout = "/Pages/Shared/_Layout.cshtml";
}

@(await Html.RenderComponentAsync<Componentes.Secciones.Noticia>(RenderMode.ServerPrerendered, new { idioma = Model.idioma, noticiaId = Model.noticia.Id }))