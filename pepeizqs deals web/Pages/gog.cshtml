@page "/gog/{id}/{nombre?}"

@model pepeizqs_deals_web.Pages.gogModel

@{
	#nullable disable

	ViewData["Title"] = Model.juego.Nombre;
	Layout = "/Pages/Shared/_Layout.cshtml";
}

@(await Html.RenderComponentAsync<Componentes.Secciones.Juego>(RenderMode.Server, new { idioma = Model.idioma, juego = Model.juego }))
