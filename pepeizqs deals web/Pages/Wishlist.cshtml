@page
@model pepeizqs_deals_web.Pages.WishlistModel

@using Microsoft.AspNetCore.Components

@{
	#nullable disable

	ViewData["Title"] = Herramientas.Idiomas.CogerCadena(Model.idioma, "Title", "Wishlist");
	Layout = "/Pages/Shared/_Layout.cshtml";
}

@(await Html.RenderComponentAsync<Componentes.Secciones.Deseados>(RenderMode.ServerPrerendered, new { 
	idioma = Model.idioma
}))
