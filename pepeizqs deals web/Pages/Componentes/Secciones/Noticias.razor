

@code {

	#nullable disable

	protected override void OnInitialized()
	{
		foreach (var suscripcion in BaseDatos.Suscripciones.Buscar.Todos())
		{
			if (DateTime.Now >= suscripcion.FechaEmpieza && DateTime.Now <= suscripcion.FechaTermina)
			{
			}
		}
	}

}
