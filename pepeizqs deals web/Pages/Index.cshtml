@page
@model IndexModel

@{
	ViewData["Title"] = Herramientas.Idiomas.CogerCadena(Model.idioma, "Index.Title");
	Layout = "/Pages/Shared/_Layout.cshtml";
}

@(await Html.RenderComponentAsync<Componentes.Secciones.Portada>(RenderMode.Server, new { idioma = @Model.idioma }))